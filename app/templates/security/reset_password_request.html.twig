{#
    templates/security/reset_password_request.html.twig
    =====================================================

    Ce template affiche le premier formulaire du processus de réinitialisation de mot de passe.
    L'utilisateur y saisit son adresse email.
    
    Le contrôleur `SecurityController::resetPasswordRequest` gère ce formulaire.
    S'il trouve un utilisateur avec cet email, il lui envoie un lien sécurisé.

    Variable Clé :
    - `resetForm`: L'objet FormView créé à partir de `ResetPasswordRequestFormType`.
#}
{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
<div class="auth-page">
    <div class="auth-card">
        <h1 class="site-title">Mot de passe oublié</h1>

        <p class="text-center" style="margin-bottom: 1.5rem; color: var(--secondary-text-color);">
            Entrez votre adresse e-mail et nous vous enverrons un lien pour réinitialiser votre mot de passe.
        </p>

        {{ form_start(resetForm) }}
            {# 
                `form_row` est un raccourci pratique qui affiche le label, le widget et les erreurs
                pour un champ donné. Il est idéal pour les formulaires simples.
            #}
            {{ form_row(resetForm.email) }}

            <button class="btn btn--primary" style="width:100%; margin-top: 1rem;">
                Envoyer le lien de réinitialisation
            </button>
        {{ form_end(resetForm) }}

        <div class="auth-links">
            <a href="{{ path('app_login') }}">Retour à la connexion</a>
        </div>
    </div>
</div>
{% endblock %} 