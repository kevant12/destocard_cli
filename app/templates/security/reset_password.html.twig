{#
    templates/security/reset_password.html.twig
    =============================================

    Ce template affiche le formulaire final où l'utilisateur saisit son nouveau mot de passe,
    après avoir cliqué sur le lien de réinitialisation reçu par email.
    
    Le contrôleur `SecurityController::resetPassword` gère ce formulaire. Il vérifie d'abord
    la validité du token dans l'URL avant d'afficher cette page.

    Variable Clé :
    - `resetForm`: L'objet FormView créé à partir de `ResetPasswordFormType`.
#}
{% extends 'base.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
<div class="auth-page">
    <div class="auth-card">
        <h1 class="site-title">Nouveau mot de passe</h1>
        
        {{ form_start(resetForm) }}
            {{ form_row(resetForm.password) }}

            <button class="btn btn--primary" style="width:100%; margin-top: 1rem;">
                Réinitialiser le mot de passe
            </button>
        {{ form_end(resetForm) }}
    </div>
</div>
{% endblock %} 