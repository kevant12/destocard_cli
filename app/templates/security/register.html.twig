{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block meta_description %}Inscrivez-vous sur Destocard pour accéder à votre espace personnel, acheter et vendre des cartes Pokémon, et gérer votre collection.{% endblock %}

{% block body %}
<div class="auth-page">
    <div class="auth-card auth-card--register">
        <h1 class="site-title">Créer un compte</h1>

        {{ form_start(registerForm) }}
            <div class="form-grid">
                <div class="form-group form-group--span-2">
                    <label class="form-label">Civilité</label>
                    <div class="form-radio-group">
                        {{ form_widget(registerForm.civility) }}
                    </div>
                    {{ form_errors(registerForm.civility) }}
                </div>

                <div class="form-group">
                    {{ form_label(registerForm.firstname) }}
                    {{ form_widget(registerForm.firstname, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(registerForm.firstname) }}
                </div>

                <div class="form-group">
                    {{ form_label(registerForm.lastname) }}
                    {{ form_widget(registerForm.lastname, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(registerForm.lastname) }}
                </div>
            </div>

            <div class="form-group">
                {{ form_label(registerForm.email) }}
                {{ form_widget(registerForm.email, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(registerForm.email) }}
            </div>

            <div class="form-group">
                {{ form_label(registerForm.password) }}
                {{ form_widget(registerForm.password, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(registerForm.password) }}
            </div>

            <div class="form-group">
                {{ form_label(registerForm.phoneNumber) }}
                {{ form_widget(registerForm.phoneNumber, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(registerForm.phoneNumber) }}
            </div>
            
            <div class="form-group form-check">
                {{ form_widget(registerForm.agreeTerms) }}
                {{ form_label(registerForm.agreeTerms) }}
                {{ form_errors(registerForm.agreeTerms) }}
            </div>

            <button type="submit" class="btn btn--primary" style="width:100%; margin-top: 1rem;">S'inscrire</button>
        {{ form_end(registerForm) }}

        <div class="auth-links">
            <span>Déjà un compte ?</span>
            <a href="{{ path('app_login') }}">Connectez-vous</a>
        </div>
    </div>
</div>
{% endblock %} 